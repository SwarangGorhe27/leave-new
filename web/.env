# Use Vite dev-server proxy (see vite.config.ts) — requests go to /api/* on localhost:5173
# and are forwarded to the backend with Host: acme.localhost for tenant routing.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
VITE_TENANT_HOST=acme.localhost:8000
VITE_TENANT_SCHEMA=acme
